# SKILL

A collection of Agent Skills (opencode / Claude-compatible `SKILL.md` folders).
Each skill lives in its own folder and is self-contained: it has a `SKILL.md`
spec plus its own `README.md` with install and usage instructions.

Each skill folder's `README.md` documents how to install and use that skill.
